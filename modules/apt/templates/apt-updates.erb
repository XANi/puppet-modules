#!/bin/bash
# <%= @puppet_header %>
# <%=  __FILE__.gsub(/.*?puppet\//,@fqdn + ':') %>
aptitude update
aptitude autoclean
<% if @background_download %>aptitude full-upgrade --download-only -y<% end %>
